---
sensors:
  - model: awair
    name: "sala"
    endpoint:  "10.0.1.20"
store:
  type: "postgres"
  postgres:
    connection_string: "host=localhost port=5432 user=postgres password=postgres dbname=postgres sslmode=disable"